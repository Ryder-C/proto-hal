[package]
name = "proto-hal"
version = "0.1.0"
edition = "2021"

[features]
stm32 = ["pin_alternates"]

pin_alternates = []

sw_cordic = ["dep:cordic"]

[dependencies]
cordic = { version = "0.1.5", optional = true }

[dev-dependencies]
fixed = "1.28.0"

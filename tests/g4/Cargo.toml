[package]
name = "g4"
version = "0.1.0"
edition = "2024"

[dependencies]
proto-hal = { path = "../../proto-hal" }

[build-dependencies]
model = { package = "g4-model", path = "model" }
proto-hal-build = { path = "../../proto-hal-build" }
